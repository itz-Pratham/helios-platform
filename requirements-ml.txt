# ML Production Dependencies (for LSTM inference only)
# Install with: pip install -r requirements-ml.txt

# Required for LSTM model inference
tensorflow>=2.15.0,<2.19.0  # Keras model loading (match Kaggle version)
numpy>=1.24.0               # Array operations
scikit-learn>=1.3.0         # StandardScaler for feature normalization

# Optional: Faster inference (install if needed)
# tensorflow-cpu==2.15.0  # CPU-only version (smaller, faster install)
# onnxruntime>=1.15.0     # ONNX runtime (if you convert model to ONNX later)
